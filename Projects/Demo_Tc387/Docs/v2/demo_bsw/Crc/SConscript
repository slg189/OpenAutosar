# -*- coding: utf-8 -*-
"""SConscript for BSW/Crc —— 演示模块，用于冒烟验证。"""
Import('env')
try:
    Import('mod_cfg')
except Exception:
    mod_cfg = None

from autosar.module_builder import build_module

objs = build_module(env, Dir('.').abspath, 'Crc', mod_cfg)
Return('objs')
